pipeline {
    agent any 

    stages {
        stage('Print Environment Variables') {
            steps {
                // Print all environment variables
                sh 'env'
            }
        }
    }
}
